const mongodb= require('mongodb');

const conncetion= '';

mongodb.connect=(connection, {useNewUrlParser:true}, function(req,res){
module.exports=client.db;
const app= require('./app');
app.listen(3000);

})